#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	ll T, i, N;
	cin >> T;
	while(T--) {
		cin >> N;
//		ll x = N * N;
		cout << ceil(sqrt(N)) << '\n';;
	}
	return 0;
}
