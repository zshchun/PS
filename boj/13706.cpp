#include <bits/stdc++.h>
using namespace std;
int r[801];
int l[801];
int main() {
	int i;
	string s;
	cin >> s;
	cout << s << '\n';
	l[0] = 1;
	for (i=800-s.size();i>=0;i--)
		r[i] = s[i] - '0';
	int n, r;
	while (memcmp(l, r, sizeof(
		ans
	}
	return 0;
}
