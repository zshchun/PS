#include <bits/stdc++.h>
using namespace std;
int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int n, i, a, b;

	cin >> n;
	for (i=0;i<n;i++) {
		cin >> a >> b;
		cout << a + b << "\n";
	}

	return 0;
}

